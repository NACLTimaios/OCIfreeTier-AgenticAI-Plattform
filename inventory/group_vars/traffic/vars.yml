---
postgres_client_version: 16

postgres_target_host: "{{ hostvars['vm1']['ansible_host'] }}"
postgres_target_port: 5432
postgres_target_db: agentdb
postgres_target_user: agentuser

pgbench_scale: 1
pgbench_clients: 2
pgbench_jobs: 1
pgbench_time: 60

sysbench_threads: 2
sysbench_time: 60
